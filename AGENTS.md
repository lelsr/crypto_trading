# AGENTS.md

本文件是 `/Users/claw/Documents/Cryp_trading` 仓库级工作规范。所有后续开发、文档、测试、复盘都必须遵守。

## 思维原则

- 所有决策从问题本质出发，不因“惯例如此”照搬。
- 回到问题本身：要解决什么？最直接的路径是什么？从零设计会怎么做？
- 关键技术选择先查全球最新资料、官方文档和技术社区经验，再动手开发。
- 不要只会自己造轮子；能借鉴成熟项目思想时，优先借鉴，但不要过度复杂化 V1。

## 约束先行

- 新项目先写 `AGENTS.md`。
- 新目录先定结构约定：什么放哪、怎么命名、何时清理。
- 没有规范的工作空间不动手。
- 已有规范的项目，严格遵守本文件和相关 docs 中的约定。
- 大改动前先输出计划或架构确认，确认后再开发。

## 沟通方式

- 默认中文，代码、命令、变量名用英文。
- 结论先行，再给理由，不要先铺垫背景。
- 遇到模糊需求，先给最合理的方案，再问要不要调整。
- 不要问“你确定要这样吗”，除非命中红线。

## 自主边界：必须先问

以下操作即使在自动接受模式下也必须停下来问：

- 删除文件、目录或 git 历史。
- 修改 `.env`、密钥、token、CI/CD 配置。
- 数据库 schema 变更或数据迁移。
- `git push`、`git rebase`、`git reset --hard`、强制推送。
- 安装新的全局依赖或修改系统配置。
- 公开发布，例如 `npm publish`、生产部署、公开发文章。

## 通用工程纪律

- 改完主动跑测试验证，具体命令以后续项目文档为准。
- 不要为了让代码跑起来注释掉报错或加绕过标记，要找根本原因。
- 测试验证通过后主动检查项目文档是否需要同步更新。
- git 提交前对齐代码与项目文档颗粒度。
- 密钥、token、密码不进代码、不进 commit、不进日志。
- 业务代码开发必须遵守 `docs/architecture_review.md` 的分层架构。

## 项目硬约束

- V1 必须本地 Mac 可运行。
- V1 必须支持飞书提醒和 Streamlit 看板。
- V1 只做 USDT 永续合约。
- V1 只做做多入场机会判断、飞书提醒、网页看板、信号生命周期复盘。
- V1 不计算杠杆。
- V1 不实盘下单。
- V1 只预留未来下单、模拟盘、TimesFM、Agent research、组合风控扩展点。
- 不允许把项目写成单文件脚本。
- 不允许把 API 请求、指标计算、飞书推送、数据库写入混在一个文件里。

## 行情与 K线硬约束

- 活跃度排序使用多交易所 ticker。
- K线只服务于入场结构和止损位置。
- K线只在 Final Top M 之后拉取。
- K线只拉每个 symbol 的 `primary_exchange`。
- 禁止跨交易所合并 K线计算 EMA、MACD、Bollinger Bands、支撑位或任何主技术指标。
- 其他交易所 K线只允许做辅助验证，不参与主技术指标计算。
- 禁止先全市场拉 K线。
- 数据流必须先交易所内部 Top N 预裁剪，再补市值，再市值过滤，再重新排序取 Final Top M。

## 稳定性要求

- 所有外部请求必须支持 timeout、retry、rate limit、fallback cache、partial success、health status、structured logging。
- 单个交易所失败不能导致整轮扫描失败。
- 每个数据源必须记录 `source_status` 和 `latency_ms`。
- 飞书 webhook 为空时必须安全跳过。
- SQLite 写入失败不得被静默吞掉，必须进入报告和日志。

## 上下文文档

- `PLANS.md`：阶段任务和验收清单。
- `docs/plan_v0.3.md`：v0.3 产品与技术方案。
- `docs/architecture_review.md`：已确认的分层架构、数据流、事件模型、数据库、失败降级和测试计划。
- `docs/TASK.md`：当前任务状态、边界、下一步。
